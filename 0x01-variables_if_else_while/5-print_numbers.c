#include <stdio.h>

/**
 * main - print from 0 to 10.
 * Return: Always return 0 on success
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	putchar('\n');
	return (0);
}

