#include <stdio.h>
#include <string.h>

/**
 * main - prints a given phrase
 * Return: 0 if exited properly, non-zero othwrwise
 */

int main(void)
{
	 char str[51];

	 strcpy(str, "programming is like building a multilingual puzzle");
	 puts(str);

return (0);
}

